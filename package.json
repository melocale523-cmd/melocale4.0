{
  "name": "melocale4.0",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "backend:build": "cd backend && npm install && npm run build",
    "backend:start": "cd backend && npm start"
  },
  "dependencies": {},
  "devDependencies": {}
}
