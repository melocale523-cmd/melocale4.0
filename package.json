{
  "name": "react-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run build --prefix frontend && cp -r frontend/dist dist"
  },
  "dependencies": {},
  "devDependencies": {}
}
